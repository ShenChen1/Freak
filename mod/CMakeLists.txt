include(cjson)
include(nanomsg)
include(st)
include(mongoose)
include(media-server)
include(uthash)
include(ufifo)

add_compile_options(-g -Os -Wall)
add_subdirectory(utils)
add_subdirectory(proto)

link_libraries(-lrt -lpthread)
add_subdirectory(bsp)
add_subdirectory(vsf)
add_subdirectory(rtsp)